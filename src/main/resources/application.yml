server:
   port: 8081
   servlet:
      context-path: /recycle

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
      url: jdbc:mysql://127.0.0.1:3307/recycle?Unicode=true&characterEncoding=UTF-8&allowMultiQueries=true

mybatis:
   mapper-locations: classpath:mapper/*.xml
   configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
